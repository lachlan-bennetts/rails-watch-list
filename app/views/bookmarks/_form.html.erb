<div class='container'>
<h1>New bookmark</h1>
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <div class="form-inputs">
      <%= f.input :comment %>
      <%= f.input :movie_id  %>
    </div>
  <div class='d-inline'>
    <div class="form-actions">
      <%= f.button :submit, class: "btn btn-primary mt-2"%>
    </div>
  </div>
    <% end %>
  <div class='d-inline'>
    <%= link_to "Back to list", @list, class: 'btn btn-primary mt-2'  %>
  </div>
<div>
