<div class='container'>
<h1>What would you like to name the list?</h1>
  <%= simple_form_for(@list) do |f| %>
    <div class="form-inputs">
      <%= f.input :name %>>
    </div>

    <div class="form-actions">
      <%= f.button :submit, class: "btn btn-primary mt-2"%>
    </div>
  <% end %>
<div>
